def isMatch(s: str, p: str) -> bool:
    # if '?' not in p and '*' not in p: return s == p
    # p = list(p)
    # print(p)
    # for i in range(len(s)):
    #     patt = p.pop(0) if len(p) != 0 else None
    #     if i == '*':
    #         patt = p.pop(0) if len(p) != 0 else None
    #         seq = s[i]
    #         while s[i] == seq: i += 1
    #     elif i == "?":
    #         pass
    #     if s[i] != patt: return False
    # return True
    
            